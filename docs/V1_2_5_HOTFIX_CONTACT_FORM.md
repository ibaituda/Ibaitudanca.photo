# V1.2.5 Hotfix Contact Form

Fixes index contact form reload issue by loading the launch-ready JavaScript that intercepts the form submit and sends it through `/api/contact` with Resend.

Upload `index.html` to GitHub replacing the current file. The previously uploaded `api/contact.js`, `api/send-email.js`, `js/v1_2_4_launch_ready.js` and `js/v1_2_3_comms_stats_watermark.js` must remain in the project.
